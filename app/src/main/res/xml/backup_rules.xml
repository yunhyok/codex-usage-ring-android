<full-backup-content>
    <!-- Usage snapshots may contain account-derived data; keep backups opt-in and empty by default. -->
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
</full-backup-content>
